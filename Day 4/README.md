# Day 4 - Text, Date & Sparkline Functions in Excel

## Overview

Day 4 focused on learning and applying **Text Functions**, **Date & Time Functions**, and **Sparkline Charts** in Microsoft Excel for data cleaning, formatting, analysis, and visualization.

---

# Topics Learned

## Text Functions

* `TRIM()`
* `UPPER()`
* `LOWER()`
* `PROPER()`
* `LEFT()`
* `RIGHT()`
* `MID()`
* `REPLACE()`
* `SUBSTITUTE()`
* `TEXTJOIN()`
* `CONCAT()`

---

## Date & Time Functions

* `TODAY()`
* `NOW()`
* `YEAR()`
* `MONTH()`
* `DAY()`
* `HOUR()`
* `MINUTE()`
* `SECOND()`
* `NETWORKDAYS()`

---

## Sparkline Charts

* Line Sparklines
* Column Sparklines
* Profit Trend Visualization
* Mini Dashboard Display Cards

---

# Practical Tasks Performed

## Text Function Tasks

1. Remove unnecessary spaces from full names using TRIM
2. Convert names into Proper Case
3. Convert names into Upper Case
4. Generate full email IDs using CONCAT and TEXTJOIN
5. Extract first 5 characters from names
6. Extract last 5 characters from names

---

## Date & Time Tasks

1. Calculate total working days for employees
2. Extract joining year from joining date
3. Extract joining month from joining date
4. Extract joining day from joining date
5. Extract ending year from ending date
6. Extract ending month from ending date
7. Extract ending day from ending date
8. Extract hour, minutes, and seconds from joining time

---

## Sparkline Tasks

1. Add Year column using Date column
2. Add Month column based on Date
3. Add Day column using Date
4. Add Month Name column
5. Create Sparkline charts between sales values
6. Create Sparkline lines between days
7. Display Top 10 Profit Orders using Sparkline visualization cards

---

# Functions Used

* `TRIM()`
* `UPPER()`
* `LOWER()`
* `PROPER()`
* `LEFT()`
* `RIGHT()`
* `MID()`
* `REPLACE()`
* `SUBSTITUTE()`
* `TEXTJOIN()`
* `CONCAT()`
* `TODAY()`
* `NOW()`
* `YEAR()`
* `MONTH()`
* `DAY()`
* `HOUR()`
* `MINUTE()`
* `SECOND()`
* `NETWORKDAYS()`

---

# Learning Outcome

By the end of Day 4, I learned:

* Text cleaning and formatting techniques
* Date and time calculations in Excel
* Employee data analysis
* Dynamic text generation
* Creating mini visualizations using Sparklines
* Improving dashboard presentation and reporting

---

# Author
**Prince Patel**
<br>B.Tech Student | Aspiring Data Analyst

**Date:** 15-05-2026
